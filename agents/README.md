# Agent Notes

Monorepo-wide notes for future agents live here.

Use app-level notes for app-specific details:

- API: `apps/api/agents/`
- Mobile: `apps/mobile/agents/` when needed
- Web: `apps/web/agents/` when needed

## Notes

- `bruno.md`: root Bruno workspace and API smoke-test flow.
- `package-manager.md`: pnpm/Corepack/workspace notes.
- `monorepo.md`: workspace and Turbo conventions.
- `api-contract.md`: OpenAPI contract and Hey API generated client workflow.

## Maintenance Rule

When adding a new note, list it here with a one-line description.
