# Package LLM Docs

Package-specific LLM docs live here when available.

Use these files as a fast local reference before package-specific implementation work. If a file is missing, stale, or incomplete, use the package's official documentation and consider adding or updating the LLM doc afterward.

## Available

- `better-auth-llms.txt`: Better Auth reference.

## Naming

Use `<package>-llms.txt` for new package docs.
